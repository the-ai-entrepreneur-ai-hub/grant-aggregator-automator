# This workflow is disabled - use Netlify for deployment instead
# See README.md for Netlify deployment instructions

name: Deploy React App to GitHub Pages (DISABLED)

on:
  push:
    branches: [ "disabled" ]  # Changed from main to disabled
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Skip deployment
        run: echo "GitHub Pages deployment disabled. Use Netlify instead."